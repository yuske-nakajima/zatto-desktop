{
  "name": "zatto-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check": "pnpm test && pnpm build && pnpm verify:build",
    "test": "vitest run",
    "verify:build": "node tests/verify-build.mjs"
  },
  "devDependencies": {
    "jsdom": "30.0.1",
    "vite": "8.2.0",
    "vitest": "4.1.10"
  }
}
